# Domain Docs

This repo is a single context: one `CONTEXT.md` at the root and one `docs/adr/` tree alongside it. There is no `CONTEXT-MAP.md` and no per-context ADR directories under `src/`.

Skills that read or write domain documentation should target those two locations.
