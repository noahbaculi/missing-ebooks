# Copy to demo/.env and fill in. The token comes from the Cloudflare Zero Trust
# dashboard when you create the tunnel (Networks > Tunnels > your tunnel >
# Install connector > Docker: the value after `--token`).
TUNNEL_TOKEN=replace-with-your-tunnel-token
